.DS_Store
__pycache__/
*.pyc
# the ring's live runtime files (see state/README.md)
state/state
state/mood.json
state/wave.json
# your personal config, created at setup: never tracked, never touched by updates
barehands.json
classic.html
NOVROS-demo.mp4
NOVROS-settings-*.png
# Mixamo characters are Adobe's; use them, do not redistribute them.
# ** rather than * so it reaches the .anims folders too -- a single star
# does not cross a slash, so the clip libraries were slipping through.
media/**/*.glb
media/**/*.fbx
# an empty stub inherited from upstream; nothing reads it
package-lock.json
